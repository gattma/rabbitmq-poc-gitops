apiVersion: rabbitmq.com/v1beta1
kind: User
metadata:
  name: user-test
  namespace: hello-world-cluster
spec:
  tags:
    - policymaker
    - administrator
  rabbitmqClusterReference:
    name: hello-world
  importCredentialsSecret:
      name: user-test-secret # name of the secret
